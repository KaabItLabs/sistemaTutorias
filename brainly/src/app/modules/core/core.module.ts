// Angulr
import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';


@NgModule({
    declarations: [
        // Todo lo que modulo Core contiene
        // Services
        // Modules
        // Guardianes
    ],
    providers: [
        // Services
    ]
})
export class CoreModule {}